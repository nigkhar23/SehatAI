# Optional: point the UI at the live FastAPI engine for runtime fetches.
# Leave UNSET for the bulletproof static demo (reads bundled engine snapshots).
# NEXT_PUBLIC_API_BASE=https://sehat-engine.onrender.com
